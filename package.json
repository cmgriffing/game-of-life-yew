{
  "private": true,
  "scripts": {
    "compile": "cargo build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "dotenv -e .environment webpack --mode development",
    "build": "dotenv -e .environment.prod webpack --mode production",
    "start:dev": "dotenv -e .environment webpack-dev-server --mode development",
    "deploy": "git subtree push --prefix dist origin gh-pages"
  },
  "devDependencies": {
    "@wasm-tool/wasm-pack-plugin": "^1.1.0",
    "copy-webpack-plugin": "^5.1.1",
    "css-loader": "^3.5.3",
    "dotenv-cli": "^3.1.0",
    "sass": "^1.26.5",
    "sass-loader": "^8.0.2",
    "style-loader": "^1.2.1",
    "wasm-pack": "^0.9.1",
    "webpack": "^4.42.0",
    "webpack-cli": "^3.3.11",
    "webpack-dev-server": "^3.10.3"
  },
  "dependencies": {}
}
